# bookish-umbrella
run first git clone https://github.com/titaniumnetwork-dev/Ultraviolet-App.git phantom-proxy && cd phantom-proxy && npm install
then run npm start
ps create a codespace must have a account to work
